import React from "react";
import { StyleSheet, Text, View } from "react-native";
const PickUpKeyPageComponents = ({ navigation }) => {
  return (
    <View>
      <Text>하나머니 줍기 페이지</Text>
    </View>
  );
};

export default PickUpKeyPageComponents;

const styles = StyleSheet.create({});
